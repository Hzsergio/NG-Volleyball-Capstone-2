version: 1
applications:
  - frontend:
      baseDirectory: NG-Volleyball-Capstone-2/Ladder/frontend
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: NG-Volleyball-Capstone-2/Ladder/frontend/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
